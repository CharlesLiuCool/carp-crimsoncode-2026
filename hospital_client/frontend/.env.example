# Base URL of the central CARP server (for diagnosis using the aggregated model).
# If set, the Patient Diagnosis tab will call this server instead of the local backend.
# Example when running the server backend on port 8000:
VITE_CENTRAL_SERVER_URL=http://localhost:8000
