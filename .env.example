# Optional: copy to .env to override defaults.
# Used by docker-compose for the central server PostgreSQL and server_backend.

# PostgreSQL (server stack)
POSTGRES_USER=carp
POSTGRES_PASSWORD=carp
POSTGRES_DB=carp
